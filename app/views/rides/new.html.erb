<% if request.params[:format] == 'offer' %>
  <h1>New Ride Offer</h1>
<% else %>
  <h1>New Ride Request</h1>
<% end %>

<% @ride.ride_type = request.params[:format] %>
<%= render 'form', ride: @ride %>

<%= link_to 'Back', rides_path %>
