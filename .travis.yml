addons:
  postgresql: 10.1

before_install:
  - gem install bundler

language: ruby

rvm:
  - 2.4.1

dist: trusty

before_script:
#  — bundle install
#  — bundle exec rake db:create
#  — bundle exec rake db:migrate

script:
  - bundle exec rspec

after_script:
  - bundle exec rake coveralls:push
